-- Generated by aether-data-schema from crates/aether-data/runtime/schema/logical/*.toml.
-- Do not edit generated files directly; edit logical schema or explicit overrides instead.

CREATE TABLE IF NOT EXISTS public.asset_groups (
    id character varying(64) NOT NULL,
    upstream_group_id character varying(255),
    user_id character varying(64) NOT NULL,
    api_key_id character varying(64),
    provider_id character varying(64) NOT NULL,
    endpoint_id character varying(64) NOT NULL,
    key_id character varying(64) NOT NULL,
    group_type character varying(64) NOT NULL,
    name character varying(512) NOT NULL,
    description text,
    status character varying(64) NOT NULL,
    created_at_unix_secs bigint NOT NULL,
    updated_at_unix_secs bigint NOT NULL,
    deleted_at_unix_secs bigint
);

ALTER TABLE ONLY public.asset_groups ADD CONSTRAINT asset_groups_pkey PRIMARY KEY (id);
ALTER TABLE ONLY public.asset_groups ADD CONSTRAINT uq_asset_groups_upstream UNIQUE (provider_id, upstream_group_id);
CREATE INDEX IF NOT EXISTS idx_asset_groups_user_deleted_created ON public.asset_groups USING btree (user_id, deleted_at_unix_secs, created_at_unix_secs);
CREATE INDEX IF NOT EXISTS idx_asset_groups_user_type_status ON public.asset_groups USING btree (user_id, group_type, status);
ALTER TABLE ONLY public.asset_groups ADD CONSTRAINT fk_asset_groups_user FOREIGN KEY (user_id) REFERENCES public.users(id) ON DELETE CASCADE;
ALTER TABLE ONLY public.asset_groups ADD CONSTRAINT fk_asset_groups_api_key FOREIGN KEY (api_key_id) REFERENCES public.api_keys(id) ON DELETE SET NULL;
ALTER TABLE ONLY public.asset_groups ADD CONSTRAINT fk_asset_groups_provider FOREIGN KEY (provider_id) REFERENCES public.providers(id) ON DELETE RESTRICT;
ALTER TABLE ONLY public.asset_groups ADD CONSTRAINT fk_asset_groups_endpoint FOREIGN KEY (endpoint_id) REFERENCES public.provider_endpoints(id) ON DELETE RESTRICT;
ALTER TABLE ONLY public.asset_groups ADD CONSTRAINT fk_asset_groups_key FOREIGN KEY (key_id) REFERENCES public.provider_api_keys(id) ON DELETE RESTRICT;

CREATE TABLE IF NOT EXISTS public.assets (
    id character varying(64) NOT NULL,
    upstream_asset_id character varying(255),
    group_id character varying(64) NOT NULL,
    user_id character varying(64) NOT NULL,
    api_key_id character varying(64),
    asset_type character varying(64) NOT NULL,
    name character varying(512) NOT NULL,
    status character varying(64) NOT NULL,
    error_code character varying(128),
    error_message text,
    moderation jsonb,
    last_inference_at_unix_secs bigint,
    source_url_fingerprint character varying(128),
    provider_url text,
    provider_url_expires_at_unix_secs bigint,
    sanitized_metadata jsonb,
    is_deleted boolean DEFAULT false NOT NULL,
    deleted_at_unix_secs bigint,
    created_at_unix_secs bigint NOT NULL,
    updated_at_unix_secs bigint NOT NULL
);

ALTER TABLE ONLY public.assets ADD CONSTRAINT assets_pkey PRIMARY KEY (id);
ALTER TABLE ONLY public.assets ADD CONSTRAINT uq_assets_group_upstream UNIQUE (group_id, upstream_asset_id);
CREATE INDEX IF NOT EXISTS idx_assets_group_deleted_created ON public.assets USING btree (group_id, is_deleted, created_at_unix_secs);
CREATE INDEX IF NOT EXISTS idx_assets_user_type_status ON public.assets USING btree (user_id, asset_type, status);
CREATE INDEX IF NOT EXISTS idx_assets_user_deleted_created ON public.assets USING btree (user_id, is_deleted, created_at_unix_secs);
ALTER TABLE ONLY public.assets ADD CONSTRAINT fk_assets_group FOREIGN KEY (group_id) REFERENCES public.asset_groups(id) ON DELETE CASCADE;
ALTER TABLE ONLY public.assets ADD CONSTRAINT fk_assets_user FOREIGN KEY (user_id) REFERENCES public.users(id) ON DELETE CASCADE;
ALTER TABLE ONLY public.assets ADD CONSTRAINT fk_assets_api_key FOREIGN KEY (api_key_id) REFERENCES public.api_keys(id) ON DELETE SET NULL;

CREATE TABLE IF NOT EXISTS public.ark_visual_validation_sessions (
    id character varying(64) NOT NULL,
    session_id character varying(255) NOT NULL,
    user_id character varying(64) NOT NULL,
    api_key_id character varying(64),
    provider_id character varying(64) NOT NULL,
    endpoint_id character varying(64) NOT NULL,
    key_id character varying(64) NOT NULL,
    byted_token_hash character varying(128) NOT NULL,
    encrypted_byted_token text NOT NULL,
    callback_state_hash character varying(128) NOT NULL,
    status character varying(64) NOT NULL,
    expires_at_unix_secs bigint NOT NULL,
    consumed_at_unix_secs bigint,
    group_id character varying(64),
    sanitized_result jsonb,
    created_at_unix_secs bigint NOT NULL,
    updated_at_unix_secs bigint NOT NULL
);

ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT ark_visual_validation_sessions_pkey PRIMARY KEY (id);
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT uq_ark_validation_upstream UNIQUE (provider_id, session_id);
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT uq_ark_validation_callback_state UNIQUE (callback_state_hash);
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT uq_ark_validation_byted_token_hash UNIQUE (byted_token_hash);
CREATE INDEX IF NOT EXISTS idx_ark_validation_user_status_expiry ON public.ark_visual_validation_sessions USING btree (user_id, status, expires_at_unix_secs);
CREATE INDEX IF NOT EXISTS idx_ark_validation_group ON public.ark_visual_validation_sessions USING btree (group_id);
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT fk_ark_validation_user FOREIGN KEY (user_id) REFERENCES public.users(id) ON DELETE CASCADE;
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT fk_ark_validation_api_key FOREIGN KEY (api_key_id) REFERENCES public.api_keys(id) ON DELETE SET NULL;
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT fk_ark_validation_provider FOREIGN KEY (provider_id) REFERENCES public.providers(id) ON DELETE RESTRICT;
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT fk_ark_validation_endpoint FOREIGN KEY (endpoint_id) REFERENCES public.provider_endpoints(id) ON DELETE RESTRICT;
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT fk_ark_validation_key FOREIGN KEY (key_id) REFERENCES public.provider_api_keys(id) ON DELETE RESTRICT;
ALTER TABLE ONLY public.ark_visual_validation_sessions ADD CONSTRAINT fk_ark_validation_group FOREIGN KEY (group_id) REFERENCES public.asset_groups(id) ON DELETE SET NULL;
